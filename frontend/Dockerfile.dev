FROM node:20-slim

WORKDIR /app

# Copiar package.json y package-lock.json
COPY package*.json ./

# Instalar dependencias
RUN npm install

# El código fuente se montará como volumen
# No hacer COPY del código para permitir hot reload

EXPOSE 5173

# Modo desarrollo con hot reload
CMD ["npm", "run", "dev"]
